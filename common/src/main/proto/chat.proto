syntax="proto3";
option java_package="org.jgroups.relay_server";
option java_outer_classname="ChatOuter";


service Chat {
    rpc Post(stream ChatMessage) returns (stream ChatMessage);
}

message ChatMessage {
    string msg=1;
    string sender=2;
}
